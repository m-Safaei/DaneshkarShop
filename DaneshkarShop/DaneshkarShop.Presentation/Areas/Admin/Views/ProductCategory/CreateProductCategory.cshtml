@model DaneshkarShop.Domain.DTOs.AdminSide.Product.CreateProductCategoryDto

@{
    ViewData["Title"] = "افزودن ";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@* @section Scripts
{
    <script>
        function readURL(input) {

            if (input.files && input.files[0]) {
                var reader = new FileReader();

                reader.onload = function (e) {
                    $('#imgAvatar').attr('src', e.target.result);
                }

                reader.readAsDataURL(input.files[0]);
            }
        }

        $("#imgBlogUp").change(function () {
            readURL(this);
        });
    </script>

} *@

<div class="content-wrapper" style="min-height: 805px;">

    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>افزوده دسته بندی جدید </h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-left">
                        <li class="breadcrumb-item"><a href="/Admin">خانه</a></li>
                        <li class="breadcrumb-item active">افزوده دسته بندی جدید </li>
                    </ol>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>
    <section class="content">
        <form role="form" asp-action="CreateProductCategory" asp-area="Admin" asp-controller="ProductCategory" method="post" enctype="multipart/form-data">
            <div class="row">
                <div class="col-12">

                    <div class="card card-primary">
                        <div class="card-header">
                            <h3 class="card-title">دسته بندی</h3>
                        </div>
                        <!-- /.card-header -->
                        <!-- form start -->

                        <div class="card-body">
                            <div class="form-group">
                                <label for="exampleInputEmail1"> دسته بندی جدید </label>
                                <input type="text" asp-for="CategoryTitle" class="form-control" id="exampleInputEmail1" placeholder="دسته بندی خود را وارد کنید ">
                            </div>
                            <input type="hidden" asp-for="ParentId" />

                        </div>
                        <!-- /.card-body -->

                        <div class="card-footer">
                            <button type="submit" class="btn btn-primary">ارسال</button>
                        </div>
                    </div>
                </div>
                
            </div>

        </form>
    </section>

</div>
